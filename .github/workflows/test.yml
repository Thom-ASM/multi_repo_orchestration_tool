name: test-workflow
run-name: test-workflow

on:
  workflow_dispatch:

jobs:
  print-tag:
    runs-on: ubuntu-latest

    steps:
      - name: prints test out
        run: echo Test

      - name: sleeps for 30 seconds
        run: sleep 30

      - name: prints test out
        run: echo Test
